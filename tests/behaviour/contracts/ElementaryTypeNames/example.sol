pragma solidity ^0.8.6;

// SPDX-License-Identifier: MIT

contract WARP {

    function ArrayFunc() public pure returns (uint256){
        uint;
       (bool b,) = (true, uint); 
        bytes;
        uint[] memory arr = new uint[](69);
        arr[0] = arr.length;
        return arr.length;
    }
}
