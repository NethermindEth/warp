pragma solidity ^0.8.6;

// SPDX-License-Identifier: MIT

contract WARP {
    uint16[] inner = [1, 2, 3, 4];
    // uint16[][] data;
    // function test() public pure {
        // for (uint i = 1; i <= 10; i++)
        //     data.push(inner);
        // for (uint j = 1; j <= 6; j++)
        //     data.pop();
        // x = data[data.length - 1][0];
        // for (uint k = 1; k <= 4; k++)
        //     data.pop();
        // y = data[data.length - 1][1];
        // delete inner;
      // return (1,2);
    // }
}
